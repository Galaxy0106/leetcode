#include <iostream>
#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    string res;
    void helper(string s, int index){
        if(index == s.size()){
            return;
        }
        // if(s[index] == )

        // if(s[index] == '['){

        // }

    }

    string decodeString(string s) {
        int index = 0;
        helper(res, index);
        return res;
    }
};

int main(){

    return 0;
}